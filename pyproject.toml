[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "NeuroWeave"
version = "0.1.0"
description = "Implementation of Thesis"
readme = "README.md"
dependencies = [
    "panel>=1.8",
    "watchfiles>=1.1",
    "pygments>=2.19"
]


[project.urls]
Homepage = "https://github.com/username/repo"

[project.scripts]
Tool = "Tool.cli:main"